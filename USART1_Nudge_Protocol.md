# USART1 微调指令协议(`0x21` 数据段 `0x30`~`0x33`)

> 本文档**只描述"微调"这一组指令**,供上位机对接使用,自包含、可以单独发给对接的人。
> 整机 USART1 协议的其它部分(帧结构、`0x20` 状态、`0x21` 的 `0x00`/`0x01`/`0x02`、
> 抓取握手流程等)见 [`USART1_Cmd_Protocol.md`](USART1_Cmd_Protocol.md)。

- 日期:2026-09-17
- 对应下位机实现:`bsp/Src/Aa55_bsp.c`(接收)、`app/Motion_seq.c` 的 `Motion_Nudge()`

---

## 一、这个指令是干嘛的

车跑到**第 3 个停顿点**(抓取握手那一段)停下来之后,如果发现位置没对准,
由操作者**手动小步挪一下**。

- **一次微调 = 走 3 cm,自动停。** 想走 9 cm 就发三次。
- **只在第 3 个停顿点的等待窗口里可用** —— 其他时候下位机一律拒收(见 §三)。

---

## 二、帧

沿用整机帧结构,`AA 55 | type | length | payload`,**无 CRC、无帧尾**。
微调的 type 和 length **和普通指令完全一样**,只是数据段多了一套取值:

```
AA 55 21 01 CC          CC = 0x30 前   0x31 后   0x32 左   0x33 右
```

→ 总长固定 **5 字节**。

| 数据段 | 方向 | 下位机动作 |
|--------|------|-----------|
| `0x30` | 前 | 前进 **3 cm** 后停下 |
| `0x31` | 后 | 后退 3 cm |
| `0x32` | 左 | 左移 3 cm |
| `0x33` | 右 | 右移 3 cm |

> **3 cm 是下位机侧的固定值**,上位机**不传距离、也没有地方传**。
> 数据段的这 1 个字节就是方向本身(和 `0x21` 的 `0x00`/`0x01`/`0x02` 一个写法)。
>
> ⚠ 数据段的其它值(比如 `0x10`、`0x20`、`0x34`)是**非法**的,下位机丢弃并计入
> `aa55_rx_drop` —— 换句话说,**方向只能用 `0x30`~`0x33` 这 4 个**。

---

## 三、什么时候能发(门)

**只有下位机正在周期上报 `0x01`(等待颜色信息)时才接受。**

也就是只有第 3 个停顿点、抓取握手那一段的等待窗口里。

**不在这个状态时下位机静默丢弃** —— 只计进 `aa55_nudge_drop`,
**不回任何错误、也不缓存**。所以:

> **上位机必须自己判断状态,不能盲发。**

`0x20` 状态(下→上,1 字节,100 ms 周期上报)的三个取值:

| 状态字 | 含义 | 能发微调吗 |
|--------|------|-----------|
| `0x00` | 等待二维码(第 1 个停顿点) | ❌ 不能 |
| `0x01` | **等待颜色信息(第 3 个停顿点)** | ✅ **只有这一个能** |
| `0x10` | 运动中(含微调本身在走) | ❌ 不能 |

---

## 四、微调期间的实时反馈(**上位机必须配合**)

下位机在微调**期间把上报切成 `0x10`(运动中)**,走完自动切回 `0x01`:

```
下位机上报   0x01 ─────────┐                        ┌───── 0x01 ─────
                           │                        │
上位机发送        ──────► 0x30                       │
                           │                        │
(车走 3 cm,约 0.3 s)        └── 0x10 ────────────────┘
```

**副作用**:微调期间报的是 `0x10`,而"收到运动中就不要再发指令"是全局规则 ——
所以**微调期间再发会被拒**。落到行为上就是**一次一按**:

> 发出一发 → 等到**重新看到 `0x01`** → 才能发下一发。

这也顺便给了上位机一个天然的节拍:**看到 `0x01` = 上一发走完了**。

---

## 五、上位机推荐的实现

```python
# last_state / state_episode 由后台收包线程维护:
#   0x20 帧解析出状态,状态每【变化】一次 state_episode += 1

def nudge(cmd):                     # cmd = 0x30 / 0x31 / 0x32 / 0x33
    if last_state != 0x01:          # 门没开 → 直接不发
        return False, '不在等待态(需要 0x01),当前 0x%02X' % last_state

    ep = state_episode              # 记下当前是第几轮 0x01
    send(bytes([0xAA, 0x55, 0x21, 0x01, cmd]))

    # 等"离开 0x01 又回到 0x01" = 这一发走完
    t0 = now()
    saw_moving = False
    while now() - t0 < 3.0:
        if last_state == 0x10:
            saw_moving = True
        if last_state == 0x01 and (saw_moving or state_episode > ep):
            return True, ''
    return False, '超时:没等到回到 0x01'
```

### ⚠ 别写成"发完 sleep 再发下一发"

```python
# ✗ 错的(开环)
send(b'\xAA\x55\x21\x01\x30'); time.sleep(0.5)
send(b'\xAA\x55\x21\x01\x30'); time.sleep(0.5)

# 第二发会被静默丢掉(微调期间状态是 0x10、门关着)
# 结果是:你以为走了 6cm,实际只走了 3cm —— 而且没有任何报错
```

```python
# ✓ 对的(跟着状态走)
for _ in range(3):
    if not wait_until(0x01):
        break
    nudge(0x30)
```

**一句话:节拍由下位机的 `0x01` 给,不要自己定。**

---

## 六、下位机侧的参数(上位机只需知道量级)

| 项 | 值 | 说明 |
|---|---|---|
| 步长 | **3 cm** | 固定 |
| 限速 | 0.10 m/s | 比正常行驶(0.30 m/s)慢 —— **3 cm 约走 0.3 s** |
| 段超时 | 3000 ms | 卡住就停,不会一直顶着 |
| 距离判定 | 闭环 | 同一个距离环 + 1.5 cm 死区 + 连续 3 次到点判定 |

**一次微调的往返** ≈ 0.3 s 走完 + 100 ms 上报周期 → **通常 0.4~0.5 s
就能重新看到 `0x01`**。上位机的超时给 3 s 很宽裕。

> 前后微调会**顺带保持航向**(用 IMU);左右微调**不做航向修正**
> —— 这是实测结论(横移段叠加偏航修正会把横移带歪)。上位机不用管,知道有这回事即可。

---

## 七、现象对照(排障用)

| 现象 | 原因 |
|---|---|
| 发了没反应,状态一直是 `0x00` | 还没跑到第 3 个停顿点 —— **门没开** |
| 发了没反应,状态一直是 `0x10` | 车还在跑序列,或上一发微调还没走完 |
| 连按了好几次只走了一发 | 被 §四 那条挡掉了 —— 没等 `0x01` 就发了 |
| 走的方向和预期相反 | 下位机侧的前后/左右装反,不是协议问题 |
| 走的不是 3 cm | 轮子打滑 / 堵转,距离环没收敛(要下位机自查) |

---

## 八、快速验证(不用写代码,串口助手就能测)

USART1 本来就是上位机那条线,所以**状态上报你能直接看见**,自己就能判断门开没开:

```
1. 打开串口 115200 8N1,切【HEX 显示】+【HEX 发送】
   ⚠ 一定要 HEX 模式!用文本模式发 "AA55210130" 是发 ASCII 字符,下位机不认

2. 等看到        AA 55 20 01 01      ← 下位机在报 0x01,门开着,可以发
3. 发            AA 55 21 01 30      ← 往前 3cm
4. 看到          AA 55 20 01 10      ← 上报切成"运动中",说明收到了
5. 等它跳回      AA 55 20 01 01      ← 跑完停稳、门自动重开,可以再发
```

**第 4 步就是"它到底收没收到"的直接证据。**

---

## 九、下位机的诊断量(Wire 上看不到,排障时让下位机的人看)

| 变量 | 含义 |
|---|---|
| `aa55_nudge_cnt` | 通过了门的微调条数(按了不涨 = 帧没进来) |
| `aa55_nudge_drop` | 被门拒的条数(在涨 = 上位机发早了) |
| `aa55_hold_state` | 当前上报状态(微调期间应为 `0xFF`=AUTO,跑完回 `0x01`) |
| `ms_active` / `ms_prog` | 微调在跑 / 本段已走多少 |

> **`aa55_nudge_cnt` 和 `aa55_nudge_drop` 这两个数能一次分开三种情况:**
> 都没涨 = 帧没进来(线路/格式问题);`cnt` 涨 = 收到了但车不动(机械/控制);
> `drop` 涨 = 收到了但被门拒了(上位机发早了)。
