# USART1 通信协议说明

**串口参数**:115200 8N1

本文档描述 USART1 上运行的**统一 AA55 协议**。另有一套独立的 `5A A5` 协议(串口调车助手/调试用),本文档不含。

> **与原始 spec 的差异**:与 `protocol_specification(1).md` 相比,本协议**删除了 CRC16**,其余帧结构一致。
> 里程计相关的包(`0x01` / `0x10` / `0x11`)定义与 spec **完全一致**。

---

## 一、统一帧格式

```
┌────────┬────────┬────────┬──────────┐
│ 帧头   │ 类型   │ 长度   │  数据    │
│  2B    │  1B    │  1B    │  N字节   │
│ 0xAA55 │ type   │ length │ payload  │
└────────┴────────┴────────┴──────────┘
```

- **帧头**:固定 `0xAA 0x55`
- **类型**:消息类型 ID(见下表)
- **长度**:数据段长度(不含帧头、类型、长度)
- **数据**:实际数据内容
- **总长** = `4 + length`
- **字节序**:全部**小端**(低字节在前)
- **无 CRC、无帧尾**

---

## 二、方向速查表

> **方向说明**
> - **下→上**:下位机(MCU)发送给上位机(PC)
> - **上→下**:上位机(PC)发送给下位机(MCU)

| 类型 | 名称 | **方向** | 用途组 | 数据长度 | 帧长 |
|------|------|---------|--------|---------|------|
| `0x00` | 关节状态 | **下→上** | 机械臂 | 18 B | 22 |
| `0x01` | 轮式里程计 | **下→上** | 里程计 | 32 B | 36 |
| `0x02` | IMU 数据 | 下→上 | (**保留**) | 32 B | 36 |
| `0x03` | Tag 数据 | **上→下** | 机械臂 | 12 B | 16 |
| `0x04` | 关节信息 | **上→下** | 机械臂 | 18 B | 22 |
| `0x05` | 移速指令 | **上→下** | 里程计 | 12 B | 16 |
| `0x10` | 时间同步请求 | **上→下** | 里程计 | 12 B | 16 |
| `0x11` | 时间同步响应 | **下→上** | 里程计 | 28 B | 32 |

> `0x02` 按原 spec 保留给 IMU 相关通讯,**当前不使用**。

---

## 三、用途分组(对应两个任务)

### A. 里程计 / 底盘组

| 类型 | 方向 | 说明 |
|------|------|------|
| `0x01` 轮式里程计 | 下→上 | 位移增量 + 左右轮速 + **微秒时间戳**,上位机据此与 IMU 融合 |
| `0x05` 移速指令 | 上→下 | 上位机下发的底盘速度指令 |
| `0x10` 时间同步请求 | 上→下 | 上位机发起,用于对齐 MCU 时钟 |
| `0x11` 时间同步响应 | 下→上 | MCU 回带 `t1/t2/t3` |

> **时间戳是关键**:`0x01` 里的 `timestamp_us` 是 MCU 本地时钟。IMU 直连上位机、用其自己的时钟。
> **两个时钟独立**,融合前必须用 `0x10`/`0x11` 算出 offset 做对齐。

### B. 机械臂控制组

| 类型 | 方向 | 说明 |
|------|------|------|
| `0x00` 关节状态 | 下→上 | 4 个关节角度 + 夹爪 + 转盘舵机,供上位机显示 |
| `0x04` 关节信息 | 上→下 | 上位机下发的关节角度 + 夹爪 + 转盘舵机 |
| `0x03` Tag 数据 | 上→下 | 视觉/标签数据(12 字节 ASCII) |

---

## 四、详细定义

### 0x00 关节状态(下→上,18 字节)

| 偏移 | 长度 | 字段 | 类型 | 说明 |
|------|------|------|------|------|
| 0-3 | 4 | `j[0]` 关节1 | float | 弧度(6020 云台) |
| 4-7 | 4 | `j[1]` 关节2 | float | 弧度(42 步进) |
| 8-11 | 4 | `j[2]` 关节3 | float | 弧度(总线舵机1) |
| 12-15 | 4 | `j[3]` 关节4 | float | 弧度(总线舵机0) |
| 16 | 1 | `gripper` | uint8 | 0=打开,1=合上 |
| 17 | 1 | `servo1` | uint8 | 1/2/3 ↔ 0°/90°/270° |

**发送频率**:50 Hz

### 0x01 轮式里程计(下→上,32 字节)

```c
struct OdomDataPacket {
    uint64_t timestamp_us;  // 微秒时间戳（下位机本地时间）
    float delta_x;          // X方向位移增量 (米)
    float delta_y;          // Y方向位移增量 (米)
    float delta_theta;      // 角度增量 (弧度)
    float v_left;           // 左轮线速度 (米/秒)
    float v_right;          // 右轮线速度 (米/秒)
    uint32_t seq;           // 序列号（递增）
} __attribute__((packed));  // 32 字节
```

| 数据区偏移 | 长度 | 字段 | 类型 | 说明 |
|-----------|------|------|------|------|
| 0-7 | 8 | `timestamp_us` | uint64 | 下位机采集数据时的微秒时间戳 |
| 8-11 | 4 | `delta_x` | float | 相对上一次的 X 方向位移(机器人坐标系) |
| 12-15 | 4 | `delta_y` | float | 相对上一次的 Y 方向位移 |
| 16-19 | 4 | `delta_theta` | float | 相对上一次的角度变化(弧度) |
| 20-23 | 4 | `v_left` | float | 左侧轮线速度(米/秒) |
| 24-27 | 4 | `v_right` | float | 右侧轮线速度(米/秒) |
| 28-31 | 4 | `seq` | uint32 | 序列号,用于检测丢包 |

**发送频率**:200 Hz

> ⚠️ **下位机实际能力约 30 Hz**(受 CAN 总线读取限制:读一轮电机需约 20ms)。
> 200 Hz 当前硬件方案达不到,需协商或改为按实际能力下发。

> 注:`v_left` / `v_right` 为**左右两侧**的轮速平均值,**不含前后轴区分**,无法用于前后轴速度对比(打滑检测)。

### 0x02 IMU 数据(下→上,32 字节)—— **保留,当前不使用**

按原 spec 保留该类型号给 IMU 相关通讯。

### 0x03 Tag 数据(上→下,12 字节)

| 偏移 | 长度 | 字段 | 说明 |
|------|------|------|------|
| 0-11 | 12 | `data` | 12 字节 ASCII 数字,4 组 × 3 位,如 `"123456789012"` |

### 0x04 关节信息(上→下,18 字节)

结构与 `0x00` **完全一致**(方向相反):

| 偏移 | 长度 | 字段 | 类型 | 说明 |
|------|------|------|------|------|
| 0-3 | 4 | `j[0]` 关节1 | float | 弧度(6020 云台) |
| 4-7 | 4 | `j[1]` 关节2 | float | 弧度(42 步进) |
| 8-11 | 4 | `j[2]` 关节3 | float | 弧度(总线舵机1) |
| 12-15 | 4 | `j[3]` 关节4 | float | 弧度(总线舵机0) |
| 16 | 1 | `gripper` | uint8 | 0=打开,1=合上 |
| 17 | 1 | `servo1` | uint8 | 1/2/3 ↔ 0°/90°/270° |

### 0x05 移速指令(上→下,12 字节)

| 偏移 | 长度 | 字段 | 类型 | 说明 |
|------|------|------|------|------|
| 0-3 | 4 | `vx` | float | 前后,m/s |
| 4-7 | 4 | `vy` | float | 左右,m/s |
| 8-11 | 4 | `vz` | float | 旋转,rad/s |

> ⚠️ **必须周期性发送(建议 ≥20 Hz)**。系统靠"200ms 内收到有效帧"判定上位机在线,超时即停机。

### 0x10 时间同步请求(上→下,12 字节)

| 偏移 | 长度 | 字段 | 类型 | 说明 |
|------|------|------|------|------|
| 0-7 | 8 | `t1` | uint64 | 上位机发送此消息的时刻(微秒) |
| 8-11 | 4 | `seq` | uint32 | 同步序列号,用于匹配请求与响应 |

**发送频率**:0.1 Hz(每 10 秒)

### 0x11 时间同步响应(下→上,28 字节)

| 偏移 | 长度 | 字段 | 类型 | 说明 |
|------|------|------|------|------|
| 0-7 | 8 | `t1` | uint64 | 从请求复制 |
| 8-15 | 8 | `t2` | uint64 | **下位机收到请求的时刻**(微秒) |
| 16-23 | 8 | `t3` | uint64 | 下位机发送响应的时刻(微秒) |
| 24-27 | 4 | `seq` | uint32 | 从请求复制 |

### 时间同步时序

```
上位机(PC)                          下位机(MCU)
  │                                    │
  │ t1: 记录发送时间                    │
  │ ──── 0x10 TIME_SYNC_REQ ─────────> │
  │                                    │ t2: 记录接收时间(中断内打戳)
  │                                    │ t3: 记录发送时间
  │ <─── 0x11 TIME_SYNC_RESP ───────── │
  │      (t1, t2, t3, seq)             │
  │ t4: 记录接收时间                    │
  │                                    │
  │ 计算:                               │
  │ RTT    = (t4-t1) - (t3-t2)          │
  │ offset = ((t2-t1) + (t3-t4)) / 2    │
  │                                    │
  │ 时间转换: t_pc = t_mcu + offset      │
```

---

## 五、⚠️ 协议约束

### 1. 长度白名单(必须)

本协议**无 CRC**,`length` 是唯一的帧边界依据。

**接收方必须校验**:只接受下表列出的长度值,其他一律丢弃并重新查找帧头 `AA 55`。

| 合法 length | 对应包 |
|------------|--------|
| 12 | 0x03 / 0x05 / 0x10 |
| 18 | 0x00 / 0x04 |
| 28 | 0x11 |
| 32 | 0x01 / 0x02 |

### 2. 数值范围钳位(必须)

无校验字节,干扰可能让一个浮点数变成非法大值(例:`vx = 0.2` → `20.0`)。

**下位机接收后必须做范围检查**,超出合理范围直接丢弃整帧:

| 字段 | 建议上限 |
|------|---------|
| `vx` / `vy` | ±3 m/s |
| `vz` | ±5 rad/s |
| 关节角度 | ±4 rad |

### 3. 心跳

上位机需持续发送有效帧(建议 `0x05` ≥20 Hz),否则下位机判定失联并停机。

---

## 六、下位机内部换算参数(供参考)

以下参数由**下位机内部**用于把编码器计数换算成米/秒,上位机不需要:

| 参数 | 值 | 备注 |
|------|------|------|
| 前后轴距 | 0.175 m | ⚠️ 待实车实测 |
| 左右轮距 | 0.120 m | ⚠️ 待实车实测 |
| 车轮半径 | 0.0375 m | 直径 75 mm |
| 编码器分辨率 | 51200 脉冲/圈 | |

**轮序**:`[0]=FL 前左  [1]=FR 前右  [2]=RR 后右  [3]=RL 后左`
**方向约定**:车轮前进为正

---

## 七、附:另一套协议(不在本文档范围)

USART1 上还有一套 `5A A5` 开头的协议,用于**串口调车助手(调试软件)**,与本文档的 AA55 协议**互不干扰**(帧头不同),本文档不含其定义。
